---
phase: 06-resolve-brain-scan-ref
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [agents/brain-mode.md]
autonomous: true

must_haves:
  truths:
    - "brain-mode.md does not reference /brain-scan as a brain-mode artifact"
    - "brain-mode.md still lists all actual brain-mode skills (/brain-capture, /daily-note, /brain-audit, /brain-add-pattern, /brain-setup)"
    - "Empty vault guidance directs user to /brain-capture instead of /brain-scan"
  artifacts:
    - path: "agents/brain-mode.md"
      provides: "Brain mode agent definition without dangling /brain-scan references"
      contains: "/brain-capture"
  key_links: []
---

<objective>
Remove all dangling /brain-scan references from brain-mode.md. /brain-scan is an existing toolkit skill (not a brain-mode hook artifact), so brain-mode.md should not list it as something it owns or recommend it as the primary action.

Purpose: Close the final v1.0 milestone audit gap — brain-mode.md should only reference artifacts that brain-mode actually deploys.
Output: Clean brain-mode.md with no /brain-scan references.
</objective>

<execution_context>
@C:\Users\srco1\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\srco1\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/brain-mode.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove /brain-scan references from brain-mode.md</name>
  <files>agents/brain-mode.md</files>
  <action>
Make three edits to agents/brain-mode.md:

1. **Line 54** — Remove the sentence referencing /brain-scan. Change:
   "When the user asks about past work, consult the vault context injected at session start. If the relevant entry isn't there, suggest running `/brain-scan` to catalog the current project."
   To:
   "When the user asks about past work, consult the vault context injected at session start."

2. **Line 66** — Replace the empty-vault guidance. Change:
   > "Your vault is empty. Run `/brain-scan` to catalog this project and start building context."
   To:
   > "Your vault is empty. Use `/brain-capture` after your first session to start building context."

3. **Line 78** — Remove the `/brain-scan` entry from the Available Skills list entirely. Delete:
   - `/brain-scan` — Catalog the current project into the vault

Do NOT remove /brain-scan from any other file — it is a legitimate standalone toolkit skill, just not a brain-mode artifact.
  </action>
  <verify>
Run: grep -n "/brain-scan" agents/brain-mode.md
Expected: No output (zero matches).

Run: grep -c "/brain-capture" agents/brain-mode.md
Expected: Count >= 2 (the existing reference plus the new empty-vault guidance).

Run: grep "/brain-setup" agents/brain-mode.md
Expected: At least 1 match (confirming other skills were not accidentally removed).
  </verify>
  <done>brain-mode.md contains zero references to /brain-scan. All other skills remain listed. Empty-vault guidance now points to /brain-capture.</done>
</task>

</tasks>

<verification>
- `grep -rn "/brain-scan" agents/brain-mode.md` returns no matches
- `grep -c "/brain-capture" agents/brain-mode.md` returns >= 2
- All five actual brain-mode skills still listed in Available Skills section: /brain-capture, /daily-note, /brain-audit, /brain-add-pattern, /brain-setup
</verification>

<success_criteria>
brain-mode.md contains no dangling /brain-scan references. The Available Skills list is authoritative — only skills deployed by setup.sh are listed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-resolve-brain-scan-ref/06-01-SUMMARY.md`
</output>

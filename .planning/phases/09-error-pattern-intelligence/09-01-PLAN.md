---
phase: 09-error-pattern-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/lib/brain-path.sh
  - hooks/post-tool-use-failure.sh
  - agents/brain-mode.md
autonomous: true

must_haves:
  truths:
    - "After each error match, encounter_count for the matched pattern increments by 1 in pattern-store.json"
    - "When the pattern store exceeds 50 entries, the least-used patterns are pruned automatically"
    - "On first encounter of a known error, the hook injects tier=full-explanation into additionalContext"
    - "On encounters 2-4, the hook injects tier=brief-reminder into additionalContext"
    - "On encounter 5+, the hook injects tier=root-cause-flag into additionalContext"
    - "brain-mode.md instructs the agent to vary response verbosity based on the tier label"
  artifacts:
    - path: "hooks/lib/brain-path.sh"
      provides: "prune_pattern_store function"
      contains: "prune_pattern_store"
    - path: "hooks/post-tool-use-failure.sh"
      provides: "Tier calculation + safe JSON construction"
      contains: "TIER"
    - path: "agents/brain-mode.md"
      provides: "Tier-response instructions"
      contains: "tier=full-explanation"
  key_links:
    - from: "hooks/post-tool-use-failure.sh"
      to: "hooks/lib/brain-path.sh"
      via: "calls update_encounter_count then prune_pattern_store"
      pattern: "update_encounter_count.*prune_pattern_store"
    - from: "hooks/post-tool-use-failure.sh"
      to: "agents/brain-mode.md"
      via: "additionalContext tier label interpreted by agent instructions"
      pattern: "tier="
---

<objective>
Add encounter-count-aware adaptive responses and soft-cap pruning to the error pattern system.

Purpose: Brain mode should get smarter about recurring errors — full explanation on first encounter, brief reminder on repeats, root cause flag on persistent issues. The pattern store should never grow unbounded.
Output: Extended brain-path.sh (prune function), extended post-tool-use-failure.sh (tier calculation + safe JSON), extended brain-mode.md (tier-response instructions)
</objective>

<execution_context>
@C:\Users\srco1\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\srco1\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-error-pattern-intelligence/09-RESEARCH.md
@hooks/lib/brain-path.sh
@hooks/post-tool-use-failure.sh
@agents/brain-mode.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prune_pattern_store to brain-path.sh and wire into update_encounter_count</name>
  <files>hooks/lib/brain-path.sh</files>
  <action>
Add a new `prune_pattern_store` function AFTER the existing `update_encounter_count` function in `hooks/lib/brain-path.sh`. The function:

1. Accepts two args: `$1` = store_path (required), `$2` = cap (optional, default 50)
2. Reads the current pattern count with `jq '.patterns | length'`
3. If count <= cap, return 0 immediately (no work needed)
4. Otherwise, create a temp file `${store_path}.tmp.$$` and use jq to:
   - Set `.updated` to current ISO-8601 UTC timestamp
   - Replace `.patterns` with `sort_by(.encounter_count) | reverse | .[:$cap]` (keep highest-count patterns)
   - Use `--argjson cap "$cap"` and `--arg now "$now"` for safe injection
5. Atomic write via `mv "$tmp_file" "$store_path"`
6. On any jq or mv failure: `rm -f "$tmp_file"`, log error via `brain_log_error`, return 0

Then add a call to `prune_pattern_store "$store_path"` at the END of `update_encounter_count`, after the successful `mv` line (line 208-211 area), before the final `return 0`. This ensures prune only runs after a successful increment.

Follow the exact error-handling style of `update_encounter_count` — never crash, always return 0.
  </action>
  <verify>
Run: `grep -n 'prune_pattern_store' hooks/lib/brain-path.sh` — should show function definition AND the call site inside update_encounter_count.
Run: `bash -n hooks/lib/brain-path.sh` — should exit 0 (no syntax errors).
  </verify>
  <done>
`prune_pattern_store` function exists in brain-path.sh. It is called at the end of `update_encounter_count` after the atomic write. The function keeps the top-50 patterns by encounter_count and removes the rest.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tier calculation, numeric guard, and safe JSON construction to post-tool-use-failure.sh</name>
  <files>hooks/post-tool-use-failure.sh</files>
  <action>
Modify the `if [ -n "$MATCH" ]` branch (lines 42-49) to:

1. KEEP the existing `update_encounter_count "$PATTERN_STORE" "$ERROR_MSG"` call (line 44) — this must run FIRST (read-after-write for correct count).

2. AFTER the update call, read back the updated encounter count:
```bash
COUNT=$(jq -r \
  --arg err "$ERROR_MSG" \
  '.patterns[] | . as $p | select(($err | ascii_downcase) | contains($p.key | ascii_downcase)) | .encounter_count' \
  "$PATTERN_STORE" 2>/dev/null | head -1)
```

3. Add numeric guard (CRITICAL — prevents bash errors on empty/non-numeric COUNT):
```bash
COUNT="${COUNT:-0}"
if ! printf '%s' "$COUNT" | grep -qE '^[0-9]+$'; then
  COUNT=0
fi
```

4. Calculate tier with fixed thresholds:
```bash
if [ "$COUNT" -ge 5 ]; then
  TIER="root-cause-flag"
  TIER_NOTE="[Encounter $COUNT — investigate root cause, do not repeat the solution]"
elif [ "$COUNT" -ge 2 ]; then
  TIER="brief-reminder"
  TIER_NOTE="[Encounter $COUNT — give a 1-2 sentence reminder only]"
else
  TIER="full-explanation"
  TIER_NOTE="[Encounter $COUNT — give full explanation with steps]"
fi
```

5. Build the context message and JSON output using `jq --arg` (FIXES the existing Phase 4 JSON injection bug — replaces the hand-assembled string on line 48):
```bash
CONTEXT_MSG=$(printf "Past solution found [encounter_count=%s tier=%s]:\n\n%s\n\n%s" \
  "$COUNT" "$TIER" "$MATCH" "$TIER_NOTE")

OUTPUT=$(jq -n \
  --arg ctx "$CONTEXT_MSG" \
  '{"hookSpecificOutput":{"hookEventName":"PostToolUseFailure","additionalContext":$ctx}}')

emit_json "$OUTPUT"
```

6. Remove the old hand-assembled JSON line (the one with `"{\"hookSpecificOutput\"...\"$CONTEXT_MSG\"}"`)

The final structure of the match branch should be: update_encounter_count → read count → guard → tier → build context → jq JSON → emit_json → exit 0.
  </action>
  <verify>
Run: `bash -n hooks/post-tool-use-failure.sh` — should exit 0 (no syntax errors).
Run: `grep -c 'jq -n' hooks/post-tool-use-failure.sh` — should be 1 (the safe JSON construction).
Run: `grep 'TIER=' hooks/post-tool-use-failure.sh` — should show all three tier assignments.
Confirm NO remaining hand-assembled JSON with `\"hookSpecificOutput\"` pattern (the old injection-vulnerable line is gone).
  </verify>
  <done>
post-tool-use-failure.sh reads encounter count after update, calculates tier (full-explanation / brief-reminder / root-cause-flag), and constructs JSON safely via `jq --arg`. The Phase 4 JSON injection bug is fixed. Non-numeric COUNT values are guarded.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tier-response instructions to brain-mode.md</name>
  <files>agents/brain-mode.md</files>
  <action>
Find the `## Error Pattern Recognition` section in `agents/brain-mode.md`. Replace the existing content of that section (keeping the heading) with:

```markdown
## Error Pattern Recognition

When a Bash command fails, the PostToolUseFailure hook checks the error against stored patterns in `$BRAIN_PATH\brain-mode\pattern-store.json`. If a match is found, the hook injects the past solution AND a tier instruction into your context via `additionalContext`.

**Respond according to the tier:**

- **tier=full-explanation** (encounter 1): Show the full past solution with all steps. Make it prominent — this is actionable information the user needs right now: "I found a past solution for this error: [solution]".
- **tier=brief-reminder** (encounters 2-4): Give a 1-2 sentence reminder: "You've seen this before — [key fix]." Do not repeat the full explanation.
- **tier=root-cause-flag** (encounters 5+): Do NOT repeat the solution. Instead say: "This error has recurred [N] times. The recurring pattern suggests a root cause that hasn't been addressed. Let's investigate why this keeps happening rather than applying the fix again." Then proactively investigate the underlying cause.

To build the pattern store, use `/brain-add-pattern` after solving a recurring error. You can also proactively suggest adding a pattern when you notice the user fixing the same type of error repeatedly.
```

This preserves the `/brain-add-pattern` guidance from the original section while adding the three-tier response behavior.
  </action>
  <verify>
Run: `grep 'tier=full-explanation' agents/brain-mode.md` — should match.
Run: `grep 'tier=brief-reminder' agents/brain-mode.md` — should match.
Run: `grep 'tier=root-cause-flag' agents/brain-mode.md` — should match.
  </verify>
  <done>
brain-mode.md contains explicit tier-response instructions. The agent knows to give full explanations on first encounter, brief reminders on repeats 2-4, and root cause investigation flags on 5+.
  </done>
</task>

</tasks>

<verification>
1. `bash -n hooks/lib/brain-path.sh` — no syntax errors
2. `bash -n hooks/post-tool-use-failure.sh` — no syntax errors
3. `grep 'prune_pattern_store' hooks/lib/brain-path.sh` — function defined AND called from update_encounter_count
4. `grep -c 'jq -n' hooks/post-tool-use-failure.sh` — returns 1 (safe JSON construction)
5. No remaining hand-assembled JSON in post-tool-use-failure.sh (`grep '\\\"hookSpecificOutput\\\"' hooks/post-tool-use-failure.sh` returns nothing)
6. `grep 'tier=' agents/brain-mode.md` — shows all three tier labels
7. Inspect pattern-store.json before and after a triggered error to verify encounter_count increments: `jq '.patterns[] | {key, encounter_count}' "$BRAIN_PATH/brain-mode/pattern-store.json"`
</verification>

<success_criteria>
- prune_pattern_store function exists and is called after every encounter_count increment
- Tier calculation produces correct labels: full-explanation (1), brief-reminder (2-4), root-cause-flag (5+)
- JSON construction uses jq --arg (no string interpolation of solution text)
- Non-numeric COUNT values default to 0 without bash errors
- brain-mode.md contains all three tier-response instructions
- All shell scripts pass bash -n syntax check
</success_criteria>

<output>
After completion, create `.planning/phases/09-error-pattern-intelligence/09-01-SUMMARY.md`
</output>

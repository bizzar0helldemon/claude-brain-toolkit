---
phase: 07-smart-stop-hook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [hooks/stop.sh]
autonomous: true

must_haves:
  truths:
    - "A session with tool usage triggers capture (decision:block)"
    - "A session with git commits triggers capture"
    - "A session with file writes/edits triggers capture"
    - "A trivial session (no tool calls) exits silently with no output"
    - "A session with missing or absent transcript_path exits silently"
  artifacts:
    - path: "hooks/stop.sh"
      provides: "Smart stop hook with signal detection"
      contains: "TOOL_COUNT"
  key_links:
    - from: "hooks/stop.sh"
      to: "transcript_path JSONL"
      via: "jq parsing of session transcript"
      pattern: "select\\(.type == .assistant.\\)"
    - from: "hooks/stop.sh"
      to: "hooks/lib/brain-path.sh"
      via: "source for emit_json and brain_path_validate"
      pattern: "source.*brain-path\\.sh"
---

<objective>
Add signal detection to stop.sh so it only triggers brain capture when the session produced meaningful work.

Purpose: Eliminate intrusive capture prompts on trivial sessions (e.g., quick question-answer, scoping, or empty exits). The hook fired 4 times on an empty scoping session — this fix makes it silent unless real work happened.

Output: Modified hooks/stop.sh with transcript-based signal detection.
</objective>

<execution_context>
@C:\Users\srco1\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\srco1\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-smart-stop-hook/07-RESEARCH.md
@hooks/stop.sh
@hooks/lib/brain-path.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add transcript signal detection to stop.sh</name>
  <files>hooks/stop.sh</files>
  <action>
Modify hooks/stop.sh in-place. Keep the existing structure (loop guard first, then source, then brain_path_validate) but add signal detection between validation and the block emission.

The modified script should follow this exact flow:

1. Read HOOK_INPUT (existing — keep as-is)
2. Loop guard check on stop_hook_active (existing — keep as-is, do NOT move)
3. Source brain-path.sh (existing — keep as-is)
4. brain_path_validate (existing — keep as-is)
5. NEW: Extract transcript_path from HOOK_INPUT using jq
6. NEW: Default TOOL_COUNT=0, HAS_GIT_COMMIT=0, HAS_FILE_CHANGES=0
7. NEW: If transcript_path is non-empty AND file exists, parse it:
   - TOOL_COUNT: count tool_use entries in assistant messages using `jq -r 'select(.type == "assistant") | .message.content[]? | select(.type == "tool_use") | .name' | wc -l | tr -d ' '`
   - HAS_GIT_COMMIT: check Bash tool commands for "git commit" using `grep -c 'git commit' || echo 0`
   - HAS_FILE_CHANGES: count Write/Edit tool calls using `select(.name == "Write" or .name == "Edit") | .name | wc -l | tr -d ' '`
8. NEW: If all three signals are 0, exit 0 silently (no output, no logging)
9. MODIFIED: Log with signal counts: "Capture trigger fired (tools: $TOOL_COUNT, commits: $HAS_GIT_COMMIT, files: $HAS_FILE_CHANGES)"
10. Emit decision:block with existing REASON text (keep existing — do NOT change the capture prompt wording)
11. exit 0

Critical constraints:
- Do NOT move the stop_hook_active guard — it MUST stay before any source statement
- Do NOT change the REASON text or emit_json pattern — keep existing capture prompt
- Do NOT use exit 2 — use exit 0 with decision:block (per existing pattern)
- Use `2>/dev/null` on all jq calls to suppress errors on malformed lines
- Use `tr -d ' '` after wc -l to handle whitespace
- Use `|| echo 0` after grep -c to handle zero-match exit code
- Guard transcript reading with `[ -n "$TRANSCRIPT_PATH" ] && [ -f "$TRANSCRIPT_PATH" ]`

Reference the "Full Detection Logic Skeleton" in 07-RESEARCH.md — the implementation should match that structure closely.
  </action>
  <verify>
Run these checks:
1. `bash -n hooks/stop.sh` — syntax check passes (exit 0)
2. Visually confirm: stop_hook_active guard is still the first check (before source)
3. Visually confirm: script has TOOL_COUNT, HAS_GIT_COMMIT, HAS_FILE_CHANGES variables
4. Visually confirm: silent exit 0 path exists when all signals are 0
5. Visually confirm: decision:block emission path still exists with emit_json
  </verify>
  <done>
hooks/stop.sh contains signal detection logic that parses transcript_path, counts tool calls / git commits / file changes, and only emits decision:block when at least one signal is non-zero. Trivial sessions (zero signals) exit 0 with no output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate signal detection with dry-run tests</name>
  <files>hooks/stop.sh</files>
  <action>
Create temporary test JSONL files and verify the jq extraction logic works correctly. Do NOT modify stop.sh — only test the jq patterns against synthetic data.

Test 1 — Trivial session (should produce TOOL_COUNT=0):
Create a temp JSONL file with only user/assistant text messages (no tool_use), plus some progress entries. Run the TOOL_COUNT jq pattern against it. Confirm count is 0.

Test 2 — Active session (should produce TOOL_COUNT > 0):
Create a temp JSONL file with assistant messages containing tool_use entries (Bash, Read, Write). Run the TOOL_COUNT jq pattern. Confirm count matches expected.

Test 3 — Git commit detection:
Create a temp JSONL with a Bash tool_use entry where input.command contains "git commit -m 'test'". Run the HAS_GIT_COMMIT pattern. Confirm count is 1.

Test 4 — File change detection:
Create a temp JSONL with Write and Edit tool_use entries. Run the HAS_FILE_CHANGES pattern. Confirm count matches.

Test 5 — Empty/missing transcript:
Run the jq pattern against /dev/null or a non-existent path. Confirm TOOL_COUNT defaults to 0 and no errors are emitted.

All temp files should be created in /tmp and cleaned up after testing. Use the exact jq patterns from stop.sh (copy them) to ensure the test validates the actual production code.
  </action>
  <verify>
All 5 tests produce expected results:
- Test 1: TOOL_COUNT=0
- Test 2: TOOL_COUNT > 0 (matches expected number)
- Test 3: HAS_GIT_COMMIT=1
- Test 4: HAS_FILE_CHANGES > 0
- Test 5: TOOL_COUNT=0, no stderr output
  </verify>
  <done>
The jq signal detection patterns in stop.sh are verified against synthetic transcript data covering trivial sessions, active sessions, git commits, file changes, and edge cases (missing file). All patterns produce correct counts.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `bash -n hooks/stop.sh` passes
2. The stop_hook_active guard remains the first check (before source)
3. Signal detection correctly identifies trivial vs. active sessions
4. decision:block is only emitted when signals > 0
5. No output is produced for trivial sessions (silent exit 0)
6. Existing REASON text and emit_json pattern are preserved
</verification>

<success_criteria>
- hooks/stop.sh has been modified with signal detection logic
- Trivial sessions (no tool calls, no commits, no file changes) exit silently
- Active sessions (any tool calls OR git commits OR file changes) trigger capture as before
- Missing/empty transcript_path is handled gracefully (defaults to trivial = no block)
- All jq patterns validated against synthetic test data
</success_criteria>

<output>
After completion, create `.planning/phases/07-smart-stop-hook/07-01-SUMMARY.md`
</output>

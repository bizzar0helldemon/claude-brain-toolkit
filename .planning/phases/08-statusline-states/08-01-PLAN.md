---
phase: 08-statusline-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/lib/brain-path.sh
  - hooks/stop.sh
  - hooks/session-start.sh
  - hooks/post-tool-use-failure.sh
  - statusline.sh
autonomous: true

must_haves:
  truths:
    - "Statusline shows brain emoji during a normal idle session (brain active, no recent hook activity)"
    - "Statusline shows green circle + brain emoji after a session where capture ran successfully"
    - "Statusline shows red circle + brain emoji when a hook error or degraded state is detected"
    - "State transitions happen automatically as hook outcomes change — no user command needed"
    - "State resets to idle on each new session start"
  artifacts:
    - path: "hooks/lib/brain-path.sh"
      provides: "write_brain_state helper function"
      contains: "write_brain_state"
    - path: "hooks/stop.sh"
      provides: "Writes captured state after decision:block, idle on trivial skip"
      contains: "write_brain_state"
    - path: "hooks/session-start.sh"
      provides: "Resets state to idle on session start"
      contains: "write_brain_state"
    - path: "hooks/post-tool-use-failure.sh"
      provides: "Writes error state on tool failure"
      contains: "write_brain_state"
    - path: "statusline.sh"
      provides: "Reads .brain-state file and displays appropriate emoji"
      contains: "BRAIN_STATE"
  key_links:
    - from: "hooks/stop.sh"
      to: "$BRAIN_PATH/.brain-state"
      via: "write_brain_state captured"
      pattern: "write_brain_state.*captured"
    - from: "hooks/session-start.sh"
      to: "$BRAIN_PATH/.brain-state"
      via: "write_brain_state idle"
      pattern: "write_brain_state.*idle"
    - from: "hooks/post-tool-use-failure.sh"
      to: "$BRAIN_PATH/.brain-state"
      via: "write_brain_state error"
      pattern: "write_brain_state.*error"
    - from: "statusline.sh"
      to: "$BRAIN_PATH/.brain-state"
      via: "cut -d' ' -f1 to read state name"
      pattern: "cut.*brain-state"
---

<objective>
Add three distinct visual states to the statusline (idle, captured, error) driven by hook outcomes via a shared state file.

Purpose: The statusline passively communicates brain activity at a glance — the user never needs to check logs or run commands to know whether capture happened or if something went wrong.

Output: Modified hooks write state to `$BRAIN_PATH/.brain-state`; statusline reads it and displays the appropriate emoji indicator on every refresh.
</objective>

<execution_context>
@C:\Users\srco1\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\srco1\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-statusline-states/08-RESEARCH.md
@.planning/phases/07-smart-stop-hook/07-01-SUMMARY.md
@hooks/lib/brain-path.sh
@hooks/stop.sh
@hooks/session-start.sh
@hooks/post-tool-use-failure.sh
@statusline.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add write_brain_state helper and hook state writes</name>
  <files>hooks/lib/brain-path.sh, hooks/stop.sh, hooks/session-start.sh, hooks/post-tool-use-failure.sh</files>
  <action>
**brain-path.sh** — Add a `write_brain_state` function after the existing `update_encounter_count` function. The function:
- Takes one argument: state name (`idle`, `captured`, or `error`)
- Guards on `[ ! -d "${BRAIN_PATH:-}" ]` — returns 0 silently if invalid (same pattern as brain_log_error)
- Writes `"$state $timestamp"` (ISO 8601 UTC) to `$BRAIN_PATH/.brain-state` using the atomic temp+mv pattern from `update_encounter_count`
- On mv failure: removes temp file and calls `brain_log_error "BrainState" "Failed to write state: $state"`
- Always returns 0

Use this exact implementation from the research doc (08-RESEARCH.md Pattern 1).

**stop.sh** — Add two `write_brain_state` calls:
1. In the trivial-session exit path (line 62-63, where `SHOULD_CAPTURE=false`): add `write_brain_state "idle"` BEFORE `exit 0`
2. After the `emit_json "$BLOCK_JSON"` call (line 70): add `write_brain_state "captured"` BEFORE `exit 0` — state must be written AFTER emit_json, not before (see Pitfall 3 in research)

**session-start.sh** — Add `write_brain_state "idle"` immediately after `brain_path_validate` succeeds (after line 8, before `source brain-context.sh`). This resets state at the start of every new session so stale captured/error states from previous sessions don't persist.

**post-tool-use-failure.sh** — Add `write_brain_state "error"` immediately after the existing `brain_log_error "ToolFailure:$TOOL_NAME" "$ERROR_MSG"` call (after line 15). This signals degraded state when a tool failure is logged.

Update the header comment in brain-path.sh to include write_brain_state in the "Provides:" list.
  </action>
  <verify>
1. `bash -n hooks/lib/brain-path.sh` — syntax check passes
2. `bash -n hooks/stop.sh` — syntax check passes
3. `bash -n hooks/session-start.sh` — syntax check passes
4. `bash -n hooks/post-tool-use-failure.sh` — syntax check passes
5. `grep -c 'write_brain_state' hooks/lib/brain-path.sh` — returns at least 1 (function definition)
6. `grep 'write_brain_state.*captured' hooks/stop.sh` — matches (captured state after emit_json)
7. `grep 'write_brain_state.*idle' hooks/stop.sh` — matches (idle on trivial skip)
8. `grep 'write_brain_state.*idle' hooks/session-start.sh` — matches (reset on start)
9. `grep 'write_brain_state.*error' hooks/post-tool-use-failure.sh` — matches (error on failure)
10. Verify write_brain_state "captured" appears AFTER emit_json in stop.sh (ordering check)
  </verify>
  <done>
All four hook files have write_brain_state calls at the correct decision points. brain-path.sh exports the helper function. All files pass bash syntax check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update statusline to read state file and display indicators</name>
  <files>statusline.sh</files>
  <action>
Replace the current brain-mode branch in statusline.sh (which just prints the brain emoji) with state-aware display logic.

The updated brain-mode branch must:
1. Read BRAIN_STATE from `$BRAIN_PATH/.brain-state` — default to `idle` if BRAIN_PATH is unset, empty, or file doesn't exist
2. Use `cut -d' ' -f1` to extract just the state name (ignoring the timestamp)
3. Use a `case` statement to select the emoji:
   - `captured` — `\360\237\237\242\360\237\247\240` (green circle + brain)
   - `error` — `\360\237\224\264\360\237\247\240` (red circle + brain)
   - `*` (default/idle) — `\360\237\247\240` (brain only, same as current)
4. Printf the selected emoji with `[MODEL] PCT%` format (same as current)

Use the exact implementation from 08-RESEARCH.md "statusline.sh — Read State and Display Indicator" code example. The non-brain-mode else branch remains unchanged.

IMPORTANT: All emoji MUST use octal escape sequences via printf, NOT raw Unicode characters. This is the existing convention for Windows/Git Bash portability.
  </action>
  <verify>
1. `bash -n statusline.sh` — syntax check passes
2. `grep 'BRAIN_STATE' statusline.sh` — matches (state variable used)
3. `grep 'brain-state' statusline.sh` — matches (state file path referenced)
4. `grep '360.237.237.242' statusline.sh` — matches (green circle octal)
5. `grep '360.237.224.264' statusline.sh` — matches (red circle octal)
6. Test idle state: `echo '{"model":{"display_name":"Sonnet"},"context_window":{"used_percentage":25},"agent":{"name":"brain-mode"}}' | BRAIN_PATH="/nonexistent" bash statusline.sh` — should output brain emoji only (no color prefix)
7. Test non-brain-mode: `echo '{"model":{"display_name":"Sonnet"},"context_window":{"used_percentage":25},"agent":{"name":"other"}}' | bash statusline.sh` — should output model+pct without brain emoji
  </verify>
  <done>
Statusline displays three distinct states based on the .brain-state file: idle (brain only), captured (green+brain), error (red+brain). Falls back gracefully to idle when state file is absent or BRAIN_PATH is unset.
  </done>
</task>

</tasks>

<verification>
1. All 5 modified files pass `bash -n` syntax check
2. `write_brain_state` function exists in brain-path.sh with atomic write pattern
3. stop.sh writes "captured" AFTER emit_json (ordering critical per Pitfall 3)
4. stop.sh writes "idle" on trivial session skip
5. session-start.sh resets to "idle" (prevents stale state from prior sessions)
6. post-tool-use-failure.sh writes "error" after brain_log_error
7. statusline.sh reads state file with BRAIN_PATH guard and defaults to idle
8. All emoji use octal escape sequences (no raw Unicode in source)
9. Mock statusline test with no state file shows brain emoji (idle fallback)
</verification>

<success_criteria>
- The statusline shows the brain emoji (idle) when no state file exists or BRAIN_PATH is unset
- State file writes are atomic (temp+mv pattern) and never crash the hook
- State transitions are driven entirely by hook outcomes — no user action required
- All existing hook behavior is preserved (no regression in stop hook capture logic, session-start context loading, or tool failure pattern matching)
</success_criteria>

<output>
After completion, create `.planning/phases/08-statusline-states/08-01-SUMMARY.md`
</output>

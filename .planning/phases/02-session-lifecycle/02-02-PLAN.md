---
phase: 02-session-lifecycle
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - hooks/stop.sh
  - hooks/pre-compact.sh
autonomous: false

must_haves:
  truths:
    - "Stop hook triggers /brain-capture and /daily-note before session ends"
    - "Stop hook fires capture on first invocation per turn, exits immediately on second (loop guard)"
    - "Pre-compact hook triggers capture before context compaction"
    - "Capture fires on both /clear (via Stop before clear) and session end (via Stop)"
    - "Brief notification tells user what was captured"
  artifacts:
    - path: "hooks/stop.sh"
      provides: "Pre-stop capture trigger via decision:block"
      min_lines: 15
    - path: "hooks/pre-compact.sh"
      provides: "Pre-compact capture trigger"
      min_lines: 15
  key_links:
    - from: "hooks/stop.sh"
      to: "decision:block reason"
      via: "emit_json with jq -n --arg"
      pattern: "decision.*block"
    - from: "hooks/stop.sh"
      to: "/brain-capture and /daily-note skills"
      via: "reason text instruction"
      pattern: "brain-capture.*daily-note"
    - from: "hooks/pre-compact.sh"
      to: "/brain-capture skill"
      via: "additionalContext instruction"
      pattern: "brain-capture"
---

<objective>
Update the Stop and PreCompact hooks to trigger automatic knowledge capture before session end and context compaction, then verify the full Phase 2 system end-to-end.

Purpose: Ensures no session knowledge is lost. The Stop hook triggers /brain-capture and /daily-note before Claude stops. PreCompact triggers capture before context is compacted. Together with Plan 01's context injection, this completes the session lifecycle loop: load knowledge at start, capture knowledge at end.
Output: hooks/stop.sh (updated), hooks/pre-compact.sh (updated), human verification of full flow
</objective>

<execution_context>
@C:\Users\srco1\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\srco1\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-lifecycle/02-CONTEXT.md
@.planning/phases/02-session-lifecycle/02-RESEARCH.md
@.planning/phases/02-session-lifecycle/02-01-SUMMARY.md
@hooks/stop.sh
@hooks/pre-compact.sh
@hooks/lib/brain-path.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stop.sh and pre-compact.sh with capture triggers</name>
  <files>hooks/stop.sh, hooks/pre-compact.sh</files>
  <action>
**stop.sh — Replace scaffold output with capture trigger:**

Keep the existing structure intact (HOOK_INPUT read, loop guard check, brain-path.sh source and validate). Replace the scaffold `emit_json '{"status":"ok",...}'` with the capture trigger:

```
REASON="Before ending this session, please run /brain-capture to preserve any useful patterns from this conversation, then run /daily-note to log a session summary. After completing both, briefly confirm what was captured (e.g., 'Brain captured: N learnings, daily note updated') and then you can stop."
BLOCK_JSON=$(jq -n --arg reason "$REASON" '{"decision":"block","reason":$reason}')
emit_json "$BLOCK_JSON"
```

The reason text instructs Claude to:
1. Run `/brain-capture` first
2. Run `/daily-note` second
3. Show a brief notification of what was captured (per user decision: "Brain captured: 3 learnings, daily note updated" style)
4. Then stop

Use `jq -n --arg` for safe JSON construction (reason text contains quotes). Exit 0 after emit.

The loop guard is already in place from Phase 1 — on second fire (`stop_hook_active=true`), exits immediately before any sourcing. This ensures capture fires exactly once per session end.

**pre-compact.sh — Add capture trigger before compaction:**

Keep the existing structure (HOOK_INPUT, brain-path.sh source, validate). Replace the scaffold output with a similar pattern. PreCompact uses `hookSpecificOutput.additionalContext` (not decision:block) to inject an instruction for Claude to run `/brain-capture` before the compaction proceeds. The instruction should say:

```
"Context is about to be compacted. Please run /brain-capture to preserve any useful patterns before the context is reduced, then confirm briefly."
```

Build with `jq -n --arg`:
```
INSTRUCTION="Context is about to be compacted. Please run /brain-capture to preserve any useful patterns before the context is reduced."
COMPACT_JSON=$(jq -n --arg ctx "$INSTRUCTION" '{"hookSpecificOutput":{"hookEventName":"PreCompact","additionalContext":$ctx}}')
emit_json "$COMPACT_JSON"
```

Log the trigger events via `brain_log_error "Stop" "Capture trigger fired"` and `brain_log_error "PreCompact" "Capture trigger fired"` respectively.

**Important:** Do NOT change the loop guard logic in stop.sh. The `STOP_HOOK_ACTIVE` check and early exit must remain exactly as-is from Phase 1.
  </action>
  <verify>
1. `bash -n hooks/stop.sh` — no syntax errors
2. `bash -n hooks/pre-compact.sh` — no syntax errors
3. Test stop.sh first fire:
   ```bash
   echo '{"stop_hook_active":false}' | BRAIN_PATH=/tmp/test-brain bash hooks/stop.sh | jq '.decision'
   # Should output: "block"
   ```
4. Test stop.sh second fire (loop guard):
   ```bash
   echo '{"stop_hook_active":true}' | bash hooks/stop.sh
   # Should produce no output (early exit)
   ```
5. Test pre-compact.sh:
   ```bash
   echo '{"trigger":"auto"}' | BRAIN_PATH=/tmp/test-brain bash hooks/pre-compact.sh | jq '.hookSpecificOutput.additionalContext'
   # Should contain the capture instruction
   ```
6. All outputs are valid JSON (pipe through `jq .`)
  </verify>
  <done>
stop.sh emits decision:block with capture instructions on first fire, exits silently on second fire. pre-compact.sh emits additionalContext with capture instruction. Both produce valid JSON.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 2 session lifecycle: SessionStart vault context injection (Plan 01) + Stop/PreCompact capture triggers (this plan). The full loop: brain loads knowledge at session start, captures knowledge before session end or compaction.
  </what-built>
  <how-to-verify>
1. **Verify SessionStart context injection:**
   - Set BRAIN_PATH to your vault (or create a test vault with a few .md files with `project: claude-brain-toolkit` frontmatter)
   - Start a new Claude session with brain mode agent
   - Confirm: brain summary block appears showing project note counts and "Global preferences active"
   - Confirm: vault entries are injected into context (ask Claude "what brain context was loaded?")

2. **Verify token budget:**
   - Check that injected context stays reasonable (ask Claude to estimate token count of brain context)
   - If you have many vault entries, confirm some were dropped (not all loaded)

3. **Verify Stop hook capture:**
   - End a session (type `/exit` or close)
   - Confirm: Claude runs `/brain-capture` and `/daily-note` before stopping
   - Confirm: brief notification appears ("Brain captured: N learnings, daily note updated")

4. **Verify session state persistence:**
   - Check `$BRAIN_PATH/.brain-session-state.json` exists and contains valid JSON with loaded entry paths
   - Start a second session — brain should load fresh (or show delta if entries changed)

5. **Verify .brain.md loading (optional):**
   - Create a `.brain.md` file in the project root with some project-local notes
   - Start a new session — confirm .brain.md content appears in loaded context
  </how-to-verify>
  <resume-signal>Type "approved" if the session lifecycle works end-to-end, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. stop.sh emits `{"decision":"block","reason":"..."}` on first fire with capture instructions
2. stop.sh exits silently (no output) on second fire (loop guard)
3. pre-compact.sh emits `hookSpecificOutput.additionalContext` with capture instruction
4. Full lifecycle: session start loads context, session end triggers capture
5. All JSON output is valid (passes `jq .`)
</verification>

<success_criteria>
- Stop hook triggers /brain-capture and /daily-note before session ends via decision:block
- Loop guard prevents double-firing (Phase 1 pattern preserved)
- PreCompact hook triggers /brain-capture before context compaction
- Full Phase 2 lifecycle verified by human: load at start, capture at end
- Brief capture notification shown to user after skills run
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-lifecycle/02-02-SUMMARY.md`
</output>
